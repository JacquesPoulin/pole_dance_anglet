export const metadata = {
	title: 'Admin - Pole Dance Anglet',
	description: "Espace d'administration Sanity",
};

export default function StudioLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
